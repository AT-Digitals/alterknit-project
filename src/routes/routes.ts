const ROOT = "/";
const LOGIN = "/login";
const LOGOUT = "/logout";
const HOME = "/home";
const ALTERKNIT_HEADER = "/alterknit-header";
const REEGISTER_EMAIL = "/register";
const routes = {
  ROOT,
  LOGIN,
  LOGOUT,
  HOME,
  ALTERKNIT_HEADER,
  REEGISTER_EMAIL,
};

export default routes;
