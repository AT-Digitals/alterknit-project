const ROOT = "/";

// Auth Routes
const LOGIN = "/login";
const LOGOUT = "/logout";
const HOME = "/home";
const FORMFILE = "/Contact-us"

const routes = {
  ROOT,
  LOGIN,
  LOGOUT,
  HOME,
  FORMFILE,
};

export default routes;
